import styles from './StatisticCard.module.css';

const StatisticCard = ({name, value, image, color}) => {
    return (
        <div className={styles.container}>

        </div>
    )
}

export default StatisticCard;